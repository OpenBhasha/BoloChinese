// Runtime configuration placeholder.
//
// This file is served as-is during development, and is REGENERATED by the
// container entrypoint (docker-entrypoint.sh) on every start, so the deployed
// API URL can be changed from the platform's environment variables without
// rebuilding the image. Do not import it — it is loaded by index.html.
window.__APP_CONFIG__ = {
  VITE_API_BASE_URL: "__VITE_API_BASE_URL__",
};
